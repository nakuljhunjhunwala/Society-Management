export enum roles {
  RESIDENT = 'resident',
  ADMIN = 'admin',
  SECRETARY = 'secretary',
  SECURITY = 'security',
}

export enum maintenancePaymentStatus {
  PENDING = 'pending',
  PROCESSED = 'processed',
  FAILED = 'failed',
}
